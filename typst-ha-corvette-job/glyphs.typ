#let npcTemplate = text(top-edge: "baseline", "\u{e900}")
#let license = text(top-edge: "baseline", "\u{e901}")
#let npcFeature = text(top-edge: "baseline", "\u{e902}")
#let npcClass = text(top-edge: "baseline", "\u{e903}")
#let squad = text(top-edge: "baseline", "\u{e904}")
#let statusDownAndOut = text(top-edge: "baseline", "\u{e905}")
#let conditionImmobilized = text(top-edge: "baseline", "\u{e906}")
#let conditionSlow = text(top-edge: "baseline", "\u{e907}")
#let manufacturer = text(top-edge: "baseline", "\u{e908}")
#let conditionStunned = text(top-edge: "baseline", "\u{e909}")
#let conditionShredded = text(top-edge: "baseline", "\u{e90a}")
#let conditionLockOn = text(top-edge: "baseline", "\u{e90b}")
#let conditionJammed = text(top-edge: "baseline", "\u{e90c}")
#let statusShutDown = text(top-edge: "baseline", "\u{e90d}")
#let statusProne = text(top-edge: "baseline", "\u{e90e}")
#let statusInvisible = text(top-edge: "baseline", "\u{e90f}")
#let statusHidden = text(top-edge: "baseline", "\u{e910}")
#let statusExposed = text(top-edge: "baseline", "\u{e911}")
#let statusEngaged = text(top-edge: "baseline", "\u{e912}")
#let statusDangerZone = text(top-edge: "baseline", "\u{e913}")
#let activationFull = text(top-edge: "baseline", "\u{e914}")
#let activationQuick = text(top-edge: "baseline", "\u{e915}")
#let techFull = text(top-edge: "baseline", "\u{e916}")
#let fullTech = text(top-edge: "baseline", "\u{e916}")
#let techQuick = text(top-edge: "baseline", "\u{e917}")
#let quickTech = text(top-edge: "baseline", "\u{e917}")
#let invade = text(top-edge: "baseline", "\u{e917}")
#let edef = text(top-edge: "baseline", "\u{e918}")
#let downtime = text(top-edge: "baseline", "\u{e919}")
#let evasion = text(top-edge: "baseline", "\u{e91a}")
#let npcTierCustom = text(top-edge: "baseline", "\u{e91b}")
#let npcTier3 = text(top-edge: "baseline", "\u{e91c}")
#let npcTier2 = text(top-edge: "baseline", "\u{e91d}")
#let npcTier1 = text(top-edge: "baseline", "\u{e91e}")
#let freeAction = text(top-edge: "baseline", "\u{e91f}")
#let free = text(top-edge: "baseline", "\u{e91f}")
#let reaction = text(top-edge: "baseline", "\u{e920}")
#let reserveTactical = text(top-edge: "baseline", "\u{e921}")
#let reserveMech = text(top-edge: "baseline", "\u{e922}")
#let reserveResource = text(top-edge: "baseline", "\u{e960}")
#let reserveBonus = text(top-edge: "baseline", "\u{e935}")
#let systemPoint = text(top-edge: "baseline", "\u{e923}")
#let genericItem = text(top-edge: "baseline", "\u{e924}")
#let save = text(top-edge: "baseline", "\u{e925}")
#let sensor = text(top-edge: "baseline", "\u{e926}")
#let weaponProfile = text(top-edge: "baseline", "\u{e927}")
#let protocol = text(top-edge: "baseline", "\u{e928}")
#let drone = text(top-edge: "baseline", "\u{e929}")
#let skill = text(top-edge: "baseline", "\u{e92a}")
#let talent = text(top-edge: "baseline", "\u{e92b}")
#let grenade = text(top-edge: "baseline", "\u{e92c}")
#let deployable = text(top-edge: "baseline", "\u{e92d}")
#let contentManager = text(top-edge: "baseline", "\u{e92e}")
#let campaign = text(top-edge: "baseline", "\u{e92f}")
#let encounter = text(top-edge: "baseline", "\u{e930}")
#let compendium = text(top-edge: "baseline", "\u{e931}")
#let mine = text(top-edge: "baseline", "\u{e932}")
#let ship = text(top-edge: "baseline", "\u{e933}")
#let vehicle = text(top-edge: "baseline", "\u{e934}")
#let accuracy = text(top-edge: "baseline", "\u{e935}")
#let activate = text(top-edge: "baseline", "\u{e936}")
#let range = text(top-edge: "baseline", "\u{e937}")
#let burst = text(top-edge: "baseline", "\u{e938}")
#let cone = text(top-edge: "baseline", "\u{e939}")
#let line = text(top-edge: "baseline", "\u{e93a}")
#let coreBonus = text(top-edge: "baseline", "\u{e93b}")
#let burn = text(top-edge: "baseline", "\u{e93c}")
#let energy = text(top-edge: "baseline", "\u{e93d}")
#let explosive = text(top-edge: "baseline", "\u{e93e}")
#let heat = text(top-edge: "baseline", "\u{e93f}")
#let kinetic = text(top-edge: "baseline", "\u{e940}")
#let variable = text(top-edge: "baseline", "\u{e941}")
#let deactivate = text(top-edge: "baseline", "\u{e942}")
#let conditionImpaired = text(top-edge: "baseline", "\u{e943}")
#let difficulty = text(top-edge: "baseline", "\u{e943}")
#let frame = text(top-edge: "baseline", "\u{e944}")
#let melee = text(top-edge: "baseline", "\u{e945}")
#let overcharge = text(top-edge: "baseline", "\u{e946}")
#let pilot = text(top-edge: "baseline", "\u{e947}")
#let thrown = text(top-edge: "baseline", "\u{e948}")
#let reactor = text(top-edge: "baseline", "\u{e949}")
#let repair = text(top-edge: "baseline", "\u{e94a}")
#let roleArtillery = text(top-edge: "baseline", "\u{e94b}")
#let roleController = text(top-edge: "baseline", "\u{e94c}")
#let roleStriker = text(top-edge: "baseline", "\u{e94d}")
#let roleSupport = text(top-edge: "baseline", "\u{e94e}")
#let roletank = text(top-edge: "baseline", "\u{e94f}")
#let roleDefender = text(top-edge: "baseline", "\u{e94f}")
#let size1 = text(top-edge: "baseline", "\u{e950}")
#let size2 = text(top-edge: "baseline", "\u{e951}")
#let size3 = text(top-edge: "baseline", "\u{e952}")
#let size4 = text(top-edge: "baseline", "\u{e953}")
#let sizeHalf = text(top-edge: "baseline", "\u{e954}")
#let structure = text(top-edge: "baseline", "\u{e955}")
#let system = text(top-edge: "baseline", "\u{e956}")
#let mechSystem = text(top-edge: "baseline", "\u{e956}")
#let threat = text(top-edge: "baseline", "\u{e957}")
#let weaponMod = text(top-edge: "baseline", "\u{e958}")
#let trait = text(top-edge: "baseline", "\u{e959}")
#let weapon = text(top-edge: "baseline", "\u{e95a}")
#let mechWeapon = text(top-edge: "baseline", "\u{e95a}")
#let blast = text(top-edge: "baseline", "\u{e95b}")
#let rank1 = text(top-edge: "baseline", "\u{e95c}")
#let rank2 = text(top-edge: "baseline", "\u{e95d}")
#let rank3 = text(top-edge: "baseline", "\u{e95e}")
#let nestedHexagons = text(top-edge: "baseline", "\u{e95f}")
#let orbit = text(top-edge: "baseline", "\u{e960}")
#let orbital = text(top-edge: "baseline", "\u{e961}")
#let largeBeam = text(top-edge: "baseline", "\u{e962}")
#let ammo = text(top-edge: "baseline", "\u{e963}")
#let burning = text(top-edge: "baseline", "\u{e964}")
#let balance = text(top-edge: "baseline", "\u{e965}")
#let reticule = text(top-edge: "baseline", "\u{e966}")
#let spikes = text(top-edge: "baseline", "\u{e967}")
#let eclipse = text(top-edge: "baseline", "\u{e968}")
#let swordArray = text(top-edge: "baseline", "\u{e969}")
#let marker = text(top-edge: "baseline", "\u{e96a}")
#let barrage = text(top-edge: "baseline", "\u{e96b}")

// ============================================================
// Unicode Glyph Resources & Examples
// ============================================================
//
// The glyphs above (U+E900 to U+E96B) are from a custom icon font
// for the Lancer RPG. Below are resources for finding standard
// Unicode glyphs and some useful examples.
//
// === Online Resources for Unicode Glyphs ===
//
// 1. Unicode Character Table (unicode-table.com)
//    - Comprehensive searchable database of all Unicode characters
//    - Shows character, code point, HTML entity, and font support
//    - Organized by Unicode blocks and categories
//
// 2. FileFormat.info Unicode Database (fileformat.info/info/unicode)
//    - Detailed technical information for each character
//    - Cross-references to various encodings and standards
//    - Search by character, name, or code point
//
// 3. Compart Unicode Charts (www.compart.com/en/unicode)
//    - Clean interface with visual character browsing
//    - Block-by-block exploration of Unicode ranges
//    - Character property information
//
// 4. Unicode.org Official Charts (unicode.org/charts)
//    - Official Unicode Consortium character code charts
//    - PDF downloads of complete Unicode blocks
//    - Most authoritative source for Unicode standards
//
// 5. Shapecatcher (shapecatcher.com)
//    - Draw a character to find similar Unicode glyphs
//    - Useful when you know what symbol looks like but not its name
//
// === Example Standard Unicode Glyphs ===
// Source: Unicode Standard v15.0 (unicode.org)
//
// Geometric Shapes (U+25A0 to U+25FF):
#let example_square = text("\u{25A0}")        // Black Square
#let example_circle = text("\u{25CF}")        // Black Circle
#let example_triangle = text("\u{25B2}")      // Black Up-Pointing Triangle
#let example_diamond = text("\u{25C6}")       // Black Diamond
#let example_star = text("\u{2605}")          // Black Star (U+2605)
//
// Arrows (U+2190 to U+21FF):
#let example_arrow_left = text("\u{2190}")    // Leftwards Arrow
#let example_arrow_right = text("\u{2192}")   // Rightwards Arrow
#let example_arrow_up = text("\u{2191}")      // Upwards Arrow
#let example_arrow_double = text("\u{21D2}")  // Rightwards Double Arrow
//
// Mathematical Operators (U+2200 to U+22FF):
#let example_sum = text("\u{2211}")           // N-Ary Summation
#let example_product = text("\u{220F}")       // N-Ary Product
#let example_infinity = text("\u{221E}")      // Infinity
#let example_approx = text("\u{2248}")        // Almost Equal To
//
// Miscellaneous Symbols (U+2600 to U+26FF):
#let example_skull = text("\u{2620}")         // Skull and Crossbones
#let example_warning = text("\u{26A0}")       // Warning Sign
#let example_checkmark = text("\u{2713}")     // Check Mark
#let example_cross = text("\u{2717}")         // Ballot X
//
// Box Drawing (U+2500 to U+257F):
#let example_box_h = text("\u{2500}")         // Box Drawings Light Horizontal
#let example_box_v = text("\u{2502}")         // Box Drawings Light Vertical
#let example_box_corner = text("\u{250C}")    // Box Drawings Light Down and Right
//
// Dingbats (U+2700 to U+27BF):
#let example_scissors = text("\u{2702}")      // Black Scissors
#let example_pencil = text("\u{270E}")        // Lower Right Pencil
#let example_crossmark = text("\u{274C}")     // Cross Mark
//
// Technical Symbols (U+2300 to U+23FF):
#let example_command = text("\u{2318}")       // Place of Interest Sign (Command key)
#let example_option = text("\u{2325}")        // Option Key
//
// Note: Not all fonts include all Unicode characters. The glyphs above
// are from common Unicode blocks with broad font support. For specialized
// glyphs, consider using dedicated icon fonts (like Font Awesome, Material
// Icons, or custom fonts like the Lancer icons in this file).
//
// ============================================================